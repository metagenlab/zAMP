name: physeq
channels:
 - conda-forge
 - bioconda
 - biocore
 - defaults
dependencies:
 - r-base # specific version correcting "can't access ldpath" error # specific version correcting "can't access ldpath" error
 - bioconductor-phyloseq = 1.28.0
 - r-vegan
 - r-dplyr
 - r-tidyverse
 - r-stringr

name: DB_processing
channels:
 - conda-forge
 - bioconda
 - biocore
 - defaults
dependencies:
 - r-base==3.5.1[build=*_1007] # specific version correcting "can't access ldpath" error # specific version correcting "can't access ldpath" error
 - r-dplyr = 0.8.3
 - r-lattice = 0.20_35
 - r-stringr = 1.3.1
 - r-forcats = 0.3.0
 - r-tidyr = 0.8.3
